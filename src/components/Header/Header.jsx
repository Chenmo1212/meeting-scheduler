import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">Meeting Room Booking Page</h1>
        </header>
    );
};

export default Header;